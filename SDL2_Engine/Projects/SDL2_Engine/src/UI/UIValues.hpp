#pragma once

#include <cstdio>

namespace SDL2_Engine {
	namespace UI {
		//! Create a simple define for storing a cstring value
		typedef char cstring[FILENAME_MAX];

		//! Define a function typedef that can be used to create non standard UI elements
	}
}